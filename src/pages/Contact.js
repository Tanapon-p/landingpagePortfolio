import React from "react";
import { useTitle } from "../hooks/useTitle";

export const Contact = () => {
  useTitle("Contact");
  return (
    <section>
      <main>Contact</main>
    </section>
  );
};
